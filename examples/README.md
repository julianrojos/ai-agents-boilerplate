# Examples

Examples are intentionally inactive. Copy and adapt them into `.agents/` only
when they match the target repository.

- `skills/ux-heuristics`: a complete skill with supporting references.
- `react-tailwind/rules`: opinionated UI rules for a React and Tailwind project.

Examples may contain assumptions about directory layout and libraries. They are
teaching material, not universal defaults.
